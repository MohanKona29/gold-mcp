[project]
name = "gold-mcp"
version = "0.1.0"
description = "Cloud native gold data tracker for Claude MCP"
requires-python = ">=3.10"
dependencies = [
    "mcp==1.2.1",
    "yfinance>=0.2.40",
    "uvicorn>=0.22.0",
    "starlette>=0.27.0"
]

[tool.poetry]
package-mode = false
