[project]
name = "gold-mcp"
version = "0.1.0"
description = "Cloud native gold data tracker for Claude MCP"
dependencies = [
    "mcp>=0.1.0",
    "yfinance>=0.2.40"
]

[tool.poetry]
package-mode = false
