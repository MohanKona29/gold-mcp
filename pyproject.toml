[project]
name = "gold-mcp"
version = "0.1.0"
description = "Cloud native gold data tracker for Claude MCP"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.5.0",
    "yfinance>=0.2.40"
]

[tool.poetry]
package-mode = false
