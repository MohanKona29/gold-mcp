# gold-mcp v2.x runs entirely on public Yahoo Finance data.
#
# This server does not read environment variables — there is nothing
# to configure here. This file is kept as a placeholder in case the
# community contributes optional adapters that need configuration in
# the future.
